# Пароль входа в админку (/admin) — проверяет API, не React.
# После смены значения перезапуск не обновит пароль в БД автоматически (см. AGENTS.md или команду ниже).
ADMIN_PASSWORD=admin

# Для docker compose (если не заданы — подставятся значения по умолчанию из docker-compose.yml)
JWT_SECRET=dev-change-me-use-openssl-rand-hex-32
POSTGRES_PASSWORD=kitchensaratov
